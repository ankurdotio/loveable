import { Router } from 'express';

const router = Router();



/**
 * POST /api/ai/message
 * req.body= {
 * content: string
 * conversationId?: string
 * projectId: string
 * }
 */
router.post("/message",)

export default router;